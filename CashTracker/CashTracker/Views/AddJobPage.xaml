<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:CashTracker.ViewModels"
             x:Class="CashTracker.Views.AddJobPage">
    <ContentPage.BindingContext>
        <vm:AddJobViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
            <Label Text="Add a New Job" HorizontalTextAlignment="Center"/>
            <Label Text="Job Name" HorizontalTextAlignment="Center"/>
            <Entry x:Name="JobNameEntry" HorizontalOptions="Center" WidthRequest="200"/>
            <Button Text="Create Job" Command="{Binding AddJobCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>